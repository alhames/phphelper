language: php

sudo: false

php:
    - '5.5'
    - '5.6'
    - '7.0'
    - hhvm

before_script:
    - phpenv config-rm xdebug.ini

matrix:
    allow_failures:
        - php: hhvm
    fast_finish: true
